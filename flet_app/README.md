# Flet app (Arch-notions)

Runs the Flet UI on a separate port.

## Dev
- Start: `python -m flet_app.app`
- Default Flet port is controlled by Flet runtime.

## Next
This scaffold will be extended to:
- call Django DRF APIs for upload/manage/users
- render password reset (request + confirm) UI

